# unix
# Linux - same core set of unix commands

ifconfig //ipv4 ipv6
netstat //default gateway
ping //send a packet to the host at the designated ip address/hostname
tracert // examine the hops travelled by the packet

